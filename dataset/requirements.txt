# Dataset pipeline dependencies (Python >= 3.10; ffmpeg required on PATH)
huggingface_hub>=0.30
datasets>=3.0
pandas>=2.0
pyarrow>=15.0
soundfile>=0.12
numpy>=1.26
requests>=2.31
tqdm>=4.66
openpyxl>=3.1          # reads the Mendeley metadata.xlsx
openai>=1.50           # only for sources/openai_tts generation
